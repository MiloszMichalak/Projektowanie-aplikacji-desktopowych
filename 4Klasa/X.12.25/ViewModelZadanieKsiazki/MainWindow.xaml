<Window x:Class="ViewModelZadanieKsiazki.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ViewModelZadanieKsiazki"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:LibraryViewModel></local:LibraryViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <Label Grid.Row="0" Grid.Column="0" Content="Title"></Label>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Title }"></TextBox>
        <Label Grid.Row="1" Grid.Column="0" Content="Author"></Label>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Author }"></TextBox>
        <Label Grid.Row="2" Grid.Column="0" Content="Count"></Label>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Count }"></TextBox>
        <Button Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Content="Dodaj" Command="{Binding AddBookCommand}"></Button>
        <ListBox Grid.Row="4" Grid.Column="0" ItemsSource="{Binding BookStates}">
            
        </ListBox>
        
    </Grid>
</Window>
