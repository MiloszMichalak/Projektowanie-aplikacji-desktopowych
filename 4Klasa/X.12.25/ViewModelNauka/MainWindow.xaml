<Window x:Class="ViewModelNauka.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ViewModelNauka"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:TodoViewModel></vm:TodoViewModel>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <!-- <Label Grid.Row="0" Content="{Binding Counter}"></Label> -->
        <!-- <Button Grid.Row="1" Content="ZWIEKSZ" Command="{Binding IncrementCommand}"></Button> -->
        <StackPanel Grid.Row="0" Orientation="Vertical">
            <TextBox
                Text="{Binding NewText}">
            </TextBox>
            <CheckBox Content="Is Done?" IsChecked="{Binding IsDone}"></CheckBox>
        </StackPanel>
        <Button
            Grid.Row="1"
            Content="Dodaj do listy"
            Command="{Binding AddNewItemCommand}">
        </Button>
        <Button
            Grid.Row="2"
            Content="Usun z listy"
            Command="{Binding RemoveItemCommand}">
        </Button>
        <ListBox
            Grid.Row="3"
            ItemsSource="{Binding Items}"
            SelectedItem="{Binding SelectedItem}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding IsDone}"></CheckBox>
                        <Label Content="{Binding Text}" />
                        <Button Content="Usun"
                                Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}">
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>